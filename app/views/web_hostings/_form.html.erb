<%= form_for(web_hosting) do |f| %>
  <% if web_hosting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_hosting.errors.count, "error") %> prohibited this web_hosting from being saved:</h2>

      <ul>
      <% web_hosting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :context %>
    <%= f.text_area :context %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
